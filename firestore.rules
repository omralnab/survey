rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Survey responses: allow create only, no client reads/updates/deletes.
    match /pos_survey_responses/{responseId} {
      allow create: if true;
      allow read, update, delete: if false;
    }
  }
}
